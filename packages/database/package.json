{
  "name": "@sua-marca/database",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'database build pending'",
    "lint": "echo 'database lint pending'",
    "test": "echo 'database test pending'",
    "typecheck": "echo 'database typecheck pending'",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --name init",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0"
  },
  "devDependencies": {
    "prisma": "^6.7.0",
    "tsx": "^4.20.0"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
