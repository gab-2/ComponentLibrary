{
  "name": "@sua-marca-ui/core",
  "version": "0.0.2",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "prettier --check package.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "@sua-marca-ui/config-typescript": "workspace:*",
    "vitest": "^2.1.8"
  }
}
