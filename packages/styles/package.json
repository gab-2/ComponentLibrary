{
  "name": "@sua-marca-ui/styles",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "lint": "prettier --check package.json",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sua-marca-ui/tokens": "^0.1.0"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "@sua-marca-ui/config-typescript": "workspace:*"
  }
}
