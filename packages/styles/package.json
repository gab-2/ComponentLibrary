{
  "name": "@sua-marca-ui/styles",
  "version": "0.0.2",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "lint": "prettier --check package.json",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sua-marca-ui/tokens": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "@sua-marca-ui/config-typescript": "workspace:*"
  }
}
