{
  "name": "@sua-marca/angular",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -v",
    "lint": "prettier --check package.json",
    "test": "node --test",
    "typecheck": "tsc -v"
  }
}
