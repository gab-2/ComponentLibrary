{
  "name": "@sua-marca-ui/marketing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Marketing Next UI at http://localhost:3001 requires app deps in runtime environment'",
    "build": "tsc -v",
    "lint": "prettier --check package.json",
    "test": "node ../../tooling/scripts/run-vitest-safe.mjs run --passWithNoTests",
    "typecheck": "tsc -v"
  },
  "type": "module",
  "dependencies": {
    "fastify": "^5.3.2",
    "@sua-marca-ui/ui-internal": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9",
    "@types/node": "^22.15.30"
  }
}
